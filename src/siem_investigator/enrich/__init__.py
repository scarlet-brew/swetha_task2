"""Stage 4 -- enrich with ATT&CK.

Runs over *accepted* findings rather than inside the correlation loop, which is
the ordering the assignment brief itself gives. The catalogue is derived here
from the locally retained bundle (D-03); nothing in this package opens a
network connection.
"""
