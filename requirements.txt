# Frozen dependency set for this prototype.
#
# Derived from the four dependencies declared in docs/specs/design.md 10.1 --
#   anthropic 0.84.0   streamlit 1.64.0   pydantic 2.13.5   jsonschema 4.26.0
# -- plus their transitive closure, walked from Requires-Dist metadata with this
# platform's environment markers evaluated. 4 declared + 44 transitive = 48.
#
# NOT produced by `pip freeze`. Freezing the working venv would pin whatever
# happens to be installed in it: it still held pandera 0.33.1, which 10.1 drops
# on measured grounds (its import chain touches pandas._libs.join, blocked by
# this machine's Application Control policy). A frozen set that is not derivable
# from the declared four cannot be reviewed against the design, and a package no
# declared dependency requires has no business being installed for a demo.
#
# tests/test_environment.py recomputes this closure from installed metadata and
# asserts it equals this file, so drift in either direction fails the suite.
#
# Measured on Python 3.14.2, win32. pyarrow, numpy and altair arrive with
# Streamlit and are used (10.1); pandas arrives with Streamlit and is never
# imported by this codebase.

# --- declared (design 10.1) ---
anthropic==0.84.0
jsonschema==4.26.0
pydantic==2.13.5
streamlit==1.64.0

# --- transitive closure of the four above ---
altair==6.3.0
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
distro==1.9.0
docstring-parser==0.18.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.20
itsdangerous==2.2.0
jinja2==3.1.6
jiter==0.17.0
jsonschema-specifications==2025.9.1
markupsafe==3.0.3
narwhals==2.26.0
numpy==2.5.3
packaging==26.3
pandas==3.0.6
pillow==12.3.0
protobuf==7.36.2
pyarrow==25.0.1
pydantic-core==2.46.5
pydeck==0.9.3
python-dateutil==2.9.0.post0
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
sniffio==1.3.1
starlette==1.7.0
toml==0.10.2
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.4
urllib3==2.8.0
uvicorn==0.53.0
watchdog==6.0.0
websockets==16.1.1
